import { PasswordValidatorsDirective } from './password-validators.directive';

describe('PasswordValidatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordValidatorsDirective();
    expect(directive).toBeTruthy();
  });
});
